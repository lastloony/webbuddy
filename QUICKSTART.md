# WebBuddy - Быстрый старт

## ✅ Что было сделано

Проект полностью переведён на **React + TypeScript** фронтенд с сохранением Django backend API.

### Технологии

- **Backend**: Django 5.2 + DRF + JWT
- **Frontend**: React 18 + TypeScript + Vite
- **Routing**: React Router v6
- **API**: Axios с автообновлением токенов
- **State**: React Context API

## 🚀 Первый запуск

### 1. Backend (Django)

```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# Примените миграции (если ещё не сделано)
python manage.py migrate

# Создайте суперпользователя (если ещё не сделано)
python manage.py createsuperuser

# Запустите Django
python manage.py runserver
```

Django API будет доступен на: **http://localhost:8000**

### 2. Frontend (React)

Откройте **новый терминал**:

```bash
cd frontend
npm install  # Только при первом запуске
npm run dev
```

React приложение будет доступно на: **http://localhost:5173**

### 3. Настройка данных

Откройте браузер:

1. Перейдите в админку: **http://localhost:8000/admin**
2. Войдите как суперпользователь
3. Создайте **Project**
4. Создайте **User** (пароль можно не указывать - сгенерируется автоматически)

### 4. Тестирование фронтенда

1. Откройте: **http://localhost:5173**
2. Войдите с созданным пользователем
3. При первом входе смените временный пароль
4. Создайте тестовый запрос
5. Просмотрите историю запросов

## 📦 Production build

Когда будете готовы к деплою:

```bash
# Соберите React приложение
cd frontend
npm run build

# Вернитесь в корень
cd ..

# Запустите Django - он автоматически будет отдавать React
python manage.py runserver
```

Теперь **всё приложение** доступно на **http://localhost:8000**

## 📁 Структура проекта

```
webbuddy/
├── frontend/              # React приложение
│   ├── src/
│   │   ├── components/   # UI компоненты
│   │   ├── pages/        # Страницы (Login, QueryHistory, etc)
│   │   ├── services/     # API клиент с JWT
│   │   ├── contexts/     # Auth context
│   │   └── App.tsx       # Роутинг
│   ├── package.json
│   └── vite.config.ts
│
├── users/                 # Django app - пользователи
├── queries/               # Django app - запросы и логи
├── projects/              # Django app - проекты
└── webbuddy/              # Django settings
```

## 🔑 Как работает аутентификация

### На фронтенде

1. Пользователь вводит login/password
2. React отправляет `POST /api/login/`
3. Backend возвращает `access` и `refresh` токены
4. Токены сохраняются в `localStorage`
5. Все последующие запросы автоматически добавляют `Authorization: Bearer {token}`

### Автообновление токена

- Access token живёт **24 часа**
- Refresh token живёт **7 дней**
- Когда access token истекает, фронтенд **автоматически** обновляет его через `/api/token/refresh/`
- Если refresh token истёк - редирект на `/login`

Всё это уже реализовано в `frontend/src/services/api.ts` 👍

## 🎨 Доступные страницы

| URL | Описание |
|-----|----------|
| `/login` | Страница входа |
| `/` | История запросов (главная) |
| `/queries/create` | Создание нового запроса |
| `/queries/:id` | Детали запроса с real-time логами |
| `/change-password` | Смена пароля |

Все страницы кроме `/login` защищены - требуют авторизации.

## 🛠️ Что дальше?

### Улучшения которые можно добавить:

1. **WebSocket** вместо polling для real-time обновлений
2. **React Query** для кэширования API запросов
3. **Toast notifications** для уведомлений
4. **Dark mode**
5. **Responsive design** для мобильных
6. **E2E тесты** (Playwright)

## 📝 Важные файлы

### Конфигурация

- `frontend/.env` - API URL для фронтенда
- `webbuddy/settings.py` - Django настройки (CORS, JWT, static)
- `webbuddy/urls.py` - Роутинг (API + React app)

### API клиент

- `frontend/src/services/api.ts` - Весь API код здесь
- Уже настроены interceptors для JWT
- Все методы готовы к использованию

### Аутентификация

- `frontend/src/contexts/AuthContext.tsx` - Глобальный auth state
- `frontend/src/components/ProtectedRoute.tsx` - Защита маршрутов

## 🐛 Troubleshooting

### CORS ошибки

Убедитесь что Django запущен на `localhost:8000` и в `settings.py`:
```python
CORS_ALLOW_ALL_ORIGINS = True  # Для development
```

### 401 Unauthorized

- Попробуйте logout и login заново
- Проверьте что токены в localStorage актуальны
- Откройте DevTools → Application → Local Storage

### Frontend не компилируется

```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

## 🎉 Готово!

Теперь у вас современный React + TypeScript фронтенд с:

✅ JWT аутентификацией с автообновлением
✅ Protected routes
✅ Real-time обновлениями (polling)
✅ Чистой архитектурой
✅ TypeScript типизацией
✅ Ready для production

**Вопросы?** Смотрите полный README.md или документацию в `frontend/README.md`